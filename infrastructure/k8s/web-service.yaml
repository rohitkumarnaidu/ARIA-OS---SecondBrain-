apiVersion: v1
kind: Service
metadata:
  name: aria-web
  namespace: aria-os
  labels:
    app: aria-web
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http-web
  selector:
    app: aria-web
