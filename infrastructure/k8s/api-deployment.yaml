apiVersion: apps/v1
kind: Deployment
metadata:
  name: aria-api
  namespace: aria-os
  labels:
    app: aria-api
    tier: backend
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: aria-api
  template:
    metadata:
      labels:
        app: aria-api
    spec:
      containers:
        - name: api
          image: ghcr.io/aria-os/api:latest
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: aria-config
            - secretRef:
                name: aria-secrets
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 15
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
