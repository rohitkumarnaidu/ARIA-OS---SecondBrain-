version: "3.8"

services:
  api:
    image: ghcr.io/app/api:latest
    restart: always
    deploy:
      replicas: 2
    environment:
      - ENVIRONMENT=production
      - LOG_LEVEL=WARN

  web:
    image: ghcr.io/app/web:latest
    restart: always
    deploy:
      replicas: 2

  scheduler:
    image: ghcr.io/app/scheduler:latest
    restart: always
