version: "3.8"

services:
  api:
    image: ghcr.io/aria-os/api:latest
    restart: always
    deploy:
      replicas: 2
    environment:
      - ENVIRONMENT=production
      - LOG_LEVEL=WARN

  web:
    image: ghcr.io/aria-os/web:latest
    restart: always
    deploy:
      replicas: 2

  scheduler:
    image: ghcr.io/aria-os/scheduler:latest
    restart: always
